class QuizzesModel {
  final String title;
  final bool isApproved;
  final bool isPending;

  const QuizzesModel({
    required this.title,
    required this.isApproved,
    required this.isPending,
  });
}
